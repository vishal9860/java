package linkedlist;

public class Linkedlist {
  Node head;
  public void insert(int data)
  {
    Node node=new node();
    node.data=data;
    if(head==null)
    {
       head=node;

    }
    else{
        Node n=head;
        while(  )
    }
  }
}
